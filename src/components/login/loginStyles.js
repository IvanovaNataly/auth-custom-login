export const boxWidth =  400;

export const loginStyles = {
    container: {
        width: boxWidth,
        flexBasis: boxWidth,
    },
    textField: {
        height: 80,
        marginBottom: 1.5
    }
}
